package chap13.textbook.s130402;

public class Util {
public static<K,Y> boolean compare (Pair<K, V> P1, Pair<K,V>P2) {
	boolean valueCompare = p1.getKey(), equal(p2.getkey());
	boolean valueCompare =p1.getValue().equals(p2.getValue());
	return keyCompare && valueCompare;
}
}
